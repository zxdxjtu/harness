# Module: {module-name}

> Auto-generated by `/decompose`. 人工可编辑，随代码提交版本控制。

## Path: {relative/path/to/module}
## Last Updated: {ISO timestamp}

## Responsibilities

- {Primary responsibility 1}
- {Primary responsibility 2}

## Boundaries

- **Owns**: {files/directories this module owns, e.g. "src/auth/**"}
- **Depends On**: {modules this module imports from, e.g. ["src/db", "src/config"]}
- **Depended By**: {modules that import from this module, e.g. ["src/api", "src/middleware"]}
- **Forbidden Dependencies**: {modules this should NEVER import, e.g. ["src/ui", "src/cli"]}

## Quality Standards

- Max file size: {lines, e.g. 400}
- Max function complexity: {cyclomatic complexity, e.g. 10}
- Required patterns: {e.g. "all exports must have JSDoc", "error handling on all async"}
- Forbidden patterns: {e.g. "no direct DOM manipulation", "no global mutable state"}

## Interface Contract

- **Exports**: {public API surface — function names, types}
- **Events**: {events emitted/consumed, if applicable}
- **Data Flow**: {input/output data shapes}

## Change History

| Date | Change | Impact | Reviewer |
|------|--------|--------|----------|
